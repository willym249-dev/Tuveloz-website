# Itemized Health triad relay

This directory is an auditable, public-or-synthetic-only relay among Codex,
the Claude desktop session named `Revenue opportunity brainstorm`, and local
Zeo review.

Rules:

- Never place patient records, credentials, account data, or other private data here.
- A tab is connected only after it returns the exact nonce from the challenge file.
- Codex verifies Claude's reply from the remote branch, then Zeo independently
  verifies the same evidence locally.
- No AI output becomes a Zeo lesson without the owner's separate approval of the
  exact lesson text.

