{
  "//": "Editor default. Type-checking for CI runs through the split projects: `npm run typecheck` checks tsconfig.server.json (workerd) and tsconfig.client.json (DOM) separately. See those files.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "incremental": true
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules", "worker-configuration.d.ts"]
}
